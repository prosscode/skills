interface:
  display_name: "Knowledge Loop · Distill Decisions"
  short_description: "Distill one or all modules from Git decisions"
  default_prompt: "Use $loop-distill to consolidate one registered module or all modules into current decision snapshots from Git history."

policy:
  allow_implicit_invocation: false
